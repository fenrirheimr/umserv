mixin input-field(type, placeholder, size, required, pattern, id, min, max)
    .input-fields-wrapper.py-2.px-3(class=size)
        input.input-field(type=type placeholder=placeholder, required=required pattern=pattern id=id min=min max=max)
